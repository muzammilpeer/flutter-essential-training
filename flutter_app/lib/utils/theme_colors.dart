import 'package:flutter/material.dart';

class ThemeColors {
  static const chatInputColor = Color(0x00ffff9f);
  static const primaryColor = Colors.deepPurple;
  static const canvasColor = Colors.transparent;
  static const chatBubbleColor = Colors.redAccent;
  static const chatBubbleTextColor = Colors.white;
  static const chatBubbleAuthorColor = Colors.blueAccent;
  static const chatBubbleAuthorTextColor = Colors.white;
}
